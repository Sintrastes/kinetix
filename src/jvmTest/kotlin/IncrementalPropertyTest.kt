
import io.kotest.core.spec.style.StringSpec

class IncrementalPropertyTest : StringSpec({
    // "incremental sum property test" {
    //     checkAll<List<Int>> { input ->
    //         val incrementalSum = input.fold(Incremental(0)) { acc, curr ->
    //             acc.map2(Incremental(curr).map { a, b -> a + b })
    //         }
    //         val expectedSum = input.sum()
    //         incrementalSum.get() shouldBe expectedSum
    //     }
    // }
})